{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git push'; then npx prettier --check . && npm run lint && npm run typecheck; fi"
          }
        ]
      }
    ]
  }
}
