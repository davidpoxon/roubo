{
  "description": "Paths under client/src that npm run lint:design-tokens skips. It held the unmigrated paths while the colour migration to DESIGN.md's semantic tokens was in flight (#1293); that migration is complete and the list is empty. Keep it empty: an entry that covers no violation fails as stale, and a new entry must never be used to get a violation past the gate.",
  "entries": []
}
