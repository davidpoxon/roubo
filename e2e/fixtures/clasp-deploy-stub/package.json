{
  "name": "@roubo/e2e-clasp-deploy-stub",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic imperative component plugin used by the Roubo E2E harness. Drives the CP-TC-028 deploy journey through the host broker (capability.query -> process.run -> reportStatus).",
  "author": "David Poxon",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --noEmit && tsup"
  },
  "dependencies": {
    "@roubo/plugin-sdk": "*"
  },
  "devDependencies": {
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vscode-jsonrpc": "8.2.1"
  }
}
