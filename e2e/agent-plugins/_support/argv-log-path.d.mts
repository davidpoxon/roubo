// Types for `argv-log-path.mjs`. That module is plain ESM rather than TypeScript
// because the extensionless stub binary loads it off disk with no build step, so
// its one export needs a declaration for the TypeScript side of the channel.
export declare const AGENT_ARGV_LOG_PATH: string;
